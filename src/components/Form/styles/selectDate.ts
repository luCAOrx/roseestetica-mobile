import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  errorMessage: {
    marginBottom: 10,
    
    textAlign: 'center',

    fontSize: 16,

    color: "#c52626",
  }
});

export default styles;